# CI validates committed pins; it does not require the newest releases.
# Validate without upgrading: python scripts/check-python-lock.py
# Deliberate dependency updates: regenerate the lock, review the full diff,
# then run backend tests in both deployment modes and dependency/security gates.
# Regenerate requirements.lock after edits:
# uv pip compile apps/server/requirements.txt --python-version 3.14 --upgrade --no-header --output-file apps/server/requirements.lock
flask==3.1.3
flask-cors==6.0.5
flask-limiter==4.1.1
flask-talisman==1.1.0
gunicorn==26.2.0
flask-sqlalchemy==3.1.1
greenlet==3.5.6
psycopg[binary]>=3.3.5
flask-migrate==4.1.0
PyJWT==2.14.0
resend==2.46.0
stripe==15.6.1
APScheduler==3.11.3
requests>=2.34.2
authlib>=1.8.0
webauthn>=3.0.0
PyYAML>=6.0.3

# Testing
pytest>=9.1.1
pytest-cov>=7.1.0
responses>=0.26.3

# Security scanning
bandit>=1.9.4
pip-audit>=2.10.1
